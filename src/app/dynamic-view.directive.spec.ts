import { DynamicViewDirective } from './dynamic-view.directive';

describe('DynamicViewDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicViewDirective();
    expect(directive).toBeTruthy();
  });
});
